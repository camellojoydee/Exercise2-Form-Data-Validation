@page
@model FormDataValidation.Pages.Register.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Registration[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Registration[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Registration[0].MiddleInitial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Registration[0].ContactNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Registration[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Registration[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Registration[0].UserName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Registration) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MiddleInitial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
